name: app_ventas
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.7.0  # Actualiza si es viejo
  firebase_auth: ^5.3.1  # Actualiza si es viejo
  http: ^1.2.2           # Agrega aquí si no está
  # ... otras dependencias tuyas (ej. cupertino_icons)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Para linting moderno

flutter:
  uses-material-design: true